# Frontend Environment Variables
# No secrets required — the API key stays on the backend.

# In development the Vite dev-server proxy handles /api requests.
# In production set this to your deployed backend URL (no trailing slash).
VITE_API_URL=
